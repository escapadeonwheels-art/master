#Run the command to build and push to Artefact Registry: gcloud builds submit --config cloudbuild.yaml
steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/cms-strapi', '-f', 'Dockerfile', '.']
images:
- 'gcr.io/$PROJECT_ID/cms-strapi'
